PROCEDURE "SP_DELETE_PAGE"(
	IN IN_FILE STRING
)
   LANGUAGE SQLSCRIPT
   SQL SECURITY INVOKER
   --DEFAULT SCHEMA <default_schema_name>
   AS
BEGIN
   DELETE FROM "OCR.PAGES" WHERE FILENAME = IN_FILE;
END