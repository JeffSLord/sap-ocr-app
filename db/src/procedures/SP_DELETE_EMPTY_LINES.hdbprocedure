PROCEDURE "SP_DELETE_EMPTY_LINES"(
	-- IN IN_INT INTEGER
	IN IN_FILE STRING
)
   LANGUAGE SQLSCRIPT
   SQL SECURITY INVOKER
   --DEFAULT SCHEMA <default_schema_name>
   AS
BEGIN
	delete from "OCR.LINES" where FILENAME = IN_FILE AND ltrim(rtrim("TEXT")) = '';
END